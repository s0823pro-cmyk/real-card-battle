# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
build
tmp
dist-ssr
*.local
releases/

# Android SDK / 署名（各環境の local.properties。android/ 配下を追跡する場合も漏れないよう明示）
local.properties

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor native projects
# iOSは別環境でも署名・実機ビルドを再現できるように、本体プロジェクトだけGit管理する。
# 同期済みweb assets / Pods / DerivedData / Xcode個人設定は生成物なので除外。
android/
ios/App/App/public/
ios/App/App/capacitor.config.json
ios/App/App/config*.xml
ios/App/Pods/
ios/App/Podfile
ios/App/Podfile.lock
ios/App/App.xcworkspace/
ios/App/build/
ios/App/DerivedData*/
ios/DerivedData*/
ios/**/xcuserdata/
ios/**/*.xcuserstate
ios/App/CapApp-SPM/.swiftpm/

# Environment variables
.env
.env.local
.env.production

# Android 署名（リポジトリ直下に置く場合の誤コミット防止）
keystore
keystore.jks
.vercel
.env*.local
# Xcode derived data
DerivedData*/

# Generated/reference assets that are not loaded by the app
docs/generated-assets/
public/models/
src/assets/cards/unemployed/desperate_focus_v3 2.png
